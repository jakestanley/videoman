# videoman

i have all these random clips and videos on my server i need to sort

## poetry

```
cd api
poetry config virtualenvs.in-project true
poetry install
```

## db

redis

## api

some python flask thing

## web

some html thing

to set base url, you need to provide an env file in `web/.env` and set your env variables thus:
`VITE_API_BASE_URL=https://example.com/api`

## roadmap

docker compose
